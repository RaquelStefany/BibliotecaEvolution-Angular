import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.biblioteca3ds2',
  appName: 'AppBiblioteca',
  webDir: 'dist/app-biblioteca',
  bundledWebRuntime: false
};

export default config;
